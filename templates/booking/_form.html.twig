{{ form_start(form, { attr: {'id': 'form_valid' } }) }}
    {{ form_widget(form) }}

    <div id="days" class="pb-4"> 0 Nuitée</div>
    
    <button id="bookingButton" type="submit" class="btn btn-success mb-4">Valider</button>

{{ form_end(form) }}
            	
{% block javascripts %}
{{ encore_entry_script_tags('booking') }}
{% endblock %}


